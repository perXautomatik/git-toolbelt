recursivly identify nested git repos and remove them if not already added as submodules to parrent.


function (path)
    check in path children if git is absent
        return error if no git repo found
    
    

    
    
    return 0 if only 1 git repo found in whole tree

    





    function (path)

        function (parrentGit path, subfoldertree path)
            